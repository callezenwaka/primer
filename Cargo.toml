[workspace]
members = ["cli", "app"]
default-members = ["cli"]
resolver = "2"

[profile.dist]
inherits = "release"
lto = "thin"

# ---------------------------------------------------------------------------
# cargo-dist — release distribution (CLI only)
# ---------------------------------------------------------------------------

# Config for 'dist'
[workspace.metadata.dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "msi"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Features to pass to cargo build
features = ["ai"]
# Which actions to run on pull requests
pr-run-mode = "plan"
# Path that installers should place binaries in — co-located with primer init shims
install-path = "~/.primer/bin"
# Update the user's shell config to add ~/.primer/bin to PATH after install
modify-path = true
# Whether to install an updater program
install-updater = false
